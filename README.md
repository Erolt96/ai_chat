# GPT Assistant

![Python](https://img.shields.io/badge/Python-3.10+-blue)
![License](https://img.shields.io/badge/License-MIT-green)


A local AI assistant powered by the OpenAI API. Runs locally with your own `.env` key setup.

## 🚀 Features

- Chat interface with GPT
- Custom API key via `.env`
- Easy to run locally

## 🛠️ Setup

```bash
git clone https://github.com/Erolt96/ai_chat.git
cd ai_chat
pip install -r requirements.txt

Create a .env file and inside of it
OPENAI_API_KEY=your-api-key-here

Then run the app from cmd: python app.py
